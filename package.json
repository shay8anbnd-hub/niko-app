{
  "name": "niko-edge-gateway",
  "version": "1.1.0",
  "description": "Netlify Edge gateway for routing HTTP requests to a configured upstream target",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'",
    "lint": "echo 'No lint script configured'"
  },
  "devDependencies": {
    "netlify-cli": "latest"
  }
}
